#include <stdio.h>
#include <stdlib.h>

int main()
{
    int y,m,d,t,tmp;
    scanf("%d %d %d",&y,&m,&d);
    if(y%4==0&&y%100!=0||y%400==0)
    {
        tmp=0;
    }
    else tmp=1;
    if(m==1)
    {
        t=d;
    }
    else if(m==2)
    {
        t=31+d;
    }
    else if(m==3&&tmp==0)
    {
        t=31+29+d;

        if(m==4)
        {
           t=31*2+29+d;
        }
         if(m==5)
        {
           t=31*2+30+29+d;
        }
          if(m==6)
        {
            t=31*3+30+29+d;
        }
          if(m==7)
        {
            t=31*3+30*2+29+d;
        }
          if(m==8)
        {
           t=31*4+30*2+29+d;
        }
          if(m==9)
        {
           t=31*5+30*2+29+d;
        }
          if(m==10)
        {
            t=31*5+30*3+29+d;
        }
          if(m==11)
        {
           t=31*6+30*3+29+d;
        }
          if(m==12)
        {
           t=31*6+30*4+29+d;
        }
    }
        else if(m==3&&tmp==1)
    {
        t=31+28+d;

        if(m==4)
        {
           t=31*2+28+d;
        }
         if(m==5)
        {
           t=31*2+30+28+d;
        }
          if(m==6)
        {
            t=31*3+30+28+d;
        }
          if(m==7)
        {
            t=31*3+30*2+28+d;
        }
          if(m==8)
        {
           t=31*4+30*2+28+d;
        }
          if(m==9)
        {
           t=31*5+30*2+28+d;
        }
          if(m==10)
        {
            t=31*5+30*3+28+d;
        }
          if(m==11)
        {
           t=31*6+30*3+28+d;
        }
          if(m==12)
        {
           t=31*6+30*4+28+d;
        }
    }

    printf("%d",t);
    return 0;
}
